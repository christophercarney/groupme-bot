Requirements:

Python 3.4
Pillow 2.5.3 (included in groupy)
groupyAPI (pip3 install groupyAPI)
groupme api key (see groupy api install instructions for details: http://groupy.readthedocs.org/en/latest/pages/installation.html)
steam api key (place in ~/.steam.key)